# Copy this to .env.local and fill in values.
# Never commit .env.local to git.

# Base URL of the Cloudflare Worker API.
NEXT_PUBLIC_API_URL=https://api.virtuallaunch.pro

# Set to "true" to use mock data instead of live Worker.
# Use this during local dev when Worker is not available.
NEXT_PUBLIC_MOCK_API=false
